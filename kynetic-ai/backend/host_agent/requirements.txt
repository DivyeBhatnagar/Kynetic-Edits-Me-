# Host Agent requirements — bundled into the PyInstaller binary
psutil>=5.9.8
httpx>=0.27.0
redis>=5.0.4
structlog>=24.1.0
pynvml>=11.5.0
GPUtil>=1.4.0

# PyTorch — install the correct variant for the target platform:
# GPU (CUDA 12.1):   pip install torch --index-url https://download.pytorch.org/whl/cu121
# CPU only:          pip install torch --index-url https://download.pytorch.org/whl/cpu
# The line below installs CPU torch for CI/dev; override in production packaging.
torch>=2.2.0
