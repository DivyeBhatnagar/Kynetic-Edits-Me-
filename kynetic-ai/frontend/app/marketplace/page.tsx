import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Compute Marketplace",
  description:
    "Browse GPU, CPU, RAM, and NVMe compute from verified hosts. Filter by spec, price, and region.",
};

export { default } from "./MarketplacePage";
