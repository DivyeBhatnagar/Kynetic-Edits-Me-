import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Wallet",
  description: "Manage your Kynetic AI wallet — top up, track spending, and view transaction history.",
};

export { default } from "./WalletPage";
